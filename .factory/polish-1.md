# Polish round 1 — finding closure

Candidate repaired: `c0e65c8184ba074f5ae1f6d747c96a9852c945ea`.
Repair commit deployed: `9aa5de010310c2d412a9608eb6ca3c1770e46ecc`.

| Finding | Change made | Evidence |
| --- | --- | --- |
| F-1-1 | The one-click `?demo=1` route now opens an isolated demo with a real first-round prompt, tags, 30-second timer, and **Start this sample round** above the fold. The persistent banner retains Reset demo and Start for real. | Playwright: `demo shows a real sample prompt in the first 390px viewport and starts it in one click`; live cold screenshot: `.factory/evidence/polish-1-live/demo-mobile-cold.png`; live `https://flex-practice-queue.sociobot.in/?demo=1` has `sampleVisible: true` and 390 px width in `routes-a11y.json`. |
| F-1-2 | Replaced broad SPA navigation fallback with explicit rewrites for `/demo`, `/privacy`, and `/terms`; added a styled static `404.html` and the Static Web Apps `responseOverrides` rewrite. Unknown addresses now preserve HTTP 404. | Static configuration regression test; local emulator response evidence `.factory/evidence/polish-1-local-404.headers`; live `https://flex-practice-queue.sociobot.in/missing-sheet` returned 404 and is captured in `.factory/evidence/polish-1-live/404.headers` and `404-mobile-cold.png`. |
| F-1-3 | Added the `source-schedule-untouched` claim and clean-demo test. It imports, tags, practices, stops, and exports while asserting the fixture bytes remain unchanged and no request leaves the app origin. Landing, round result, legal copy, and README consistently describe the flashcard schedule. | `npm test -- --grep @claim:source-schedule-untouched` passed from the clean clone; `.factory/claims.json`; live cold demo check at `/?demo=1`. |
| F-1-4 | Removed the untestable merchant-of-record and refund assurances from the paid section and terms. The remaining $9, named-plan, and hosted Sociobot checkout statements are covered by existing payment and saved-plan claims. | `npm test -- --grep @claim:paid-price` and `npm test -- --grep @claim:saved-plans` passed from the clean clone; cold live paid section check found no merchant/refund assertion. |
| F-1-5 | Added the `license-data-minimization` claim and recorded-response test. It seeds private local data and asserts the only verification call is a bodyless GET whose sole value is the license token. | `npm test -- --grep @claim:license-data-minimization` passed from the clean clone; `.factory/claims.json`; live privacy route check in `routes-a11y.json`. |
| F-1-6 | Made the `.apkg` limitation explicit at the import control. It tells Anki users to export a front/back/tags CSV first and links to Anki’s export guidance; the supported Anki CSV path remains read-only and claimed. | `npm test -- --grep @claim:anki-csv-import` passed from the clean clone; live demo screenshot shows the instruction: `.factory/evidence/polish-1-live/demo-mobile-cold.png`. |
| F-1-7 | Standardized the protected system name as **flashcard schedule** across the first screen, workbench, boundary, footer, legal pages, README, metadata, manifest, and catalog description. | `.factory/copy-audit.md`; live landing and demo cold checks at `https://flex-practice-queue.sociobot.in/` and `/?demo=1`. |

All findings in the only review file, `.factory/review-1.md`, are closed. No earlier review or polish files existed when that review was written.
