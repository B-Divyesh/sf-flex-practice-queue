[
  {
    "id": "demo-sandbox",
    "claim": "Try it with eight sample prompts without changing real data",
    "where": "landing action, demo banner, README",
    "test": "npm test -- --grep @claim:demo-sandbox",
    "sandbox": "fresh browser context; seed a real prompt, round, and fpq:plans value, enter /demo, mutate and reset it, select Start for real, then assert exact real IndexedDB and plans values remain, sample data is absent, and demo storage is deleted"
  },
  {
    "id": "offline-reload",
    "claim": "Works offline after the first visit",
    "where": "landing facts, README",
    "test": "npm test -- --grep @claim:offline-reload",
    "sandbox": "fresh browser context at /demo; install the service worker, set the context offline, reload, and assert all sample prompts"
  },
  {
    "id": "local-privacy",
    "claim": "Study data stays in this browser",
    "where": "landing facts, privacy page, README",
    "test": "npm test -- --grep @claim:local-privacy",
    "sandbox": "fresh browser context at /demo; record the full practice flow network log and assert every request is same-origin"
  },
  {
    "id": "csv-readonly",
    "claim": "Imports prompt CSV files without changing the selected source file",
    "where": "workbench, how it works, privacy page, README",
    "test": "npm test -- --grep @claim:csv-readonly",
    "sandbox": "demo data plus a shipped CSV fixture; compare fixture bytes before and after import and assert imported rows"
  },
  {
    "id": "source-schedule-untouched",
    "claim": "Practice never changes your flashcard schedule",
    "where": "landing scope boundary, round result, privacy page, README",
    "test": "npm test -- --grep @claim:source-schedule-untouched",
    "sandbox": "fresh /demo data plus a shipped CSV fixture; import, tag, practice, stop, and export while recording requests; assert source bytes are unchanged and every request remains same-origin"
  },
  {
    "id": "anki-csv-import",
    "claim": "Imports Anki front/back CSV files without changing the selected source file",
    "where": "workbench, README",
    "test": "npm test -- --grep @claim:anki-csv-import",
    "sandbox": "fresh /demo data plus a shipped front,back,tags fixture; compare fixture bytes before and after import and assert front maps to prompt, back to answer, and tags are kept"
  },
  {
    "id": "anki-apkg-not-supported",
    "claim": "Does not import Anki .apkg packages and tells you to export CSV first",
    "where": "import help, README",
    "test": "npm test -- --grep @claim:anki-apkg-not-supported",
    "sandbox": "fresh /demo data plus a shipped .apkg-named fixture; select it, assert no prompt is added, and assert the exact CSV export guidance"
  },
  {
    "id": "csv-export",
    "claim": "Exports every prompt as CSV",
    "where": "workbench, privacy page, README",
    "test": "npm test -- --grep @claim:csv-export",
    "sandbox": "fresh /demo data; download CSV and assert its heading, filename, row count, and a sample prompt"
  },
  {
    "id": "mixed-round",
    "claim": "Runs timed mixed rounds with keyboard controls",
    "where": "landing, demo, how it works, README",
    "test": "npm test -- --grep @claim:mixed-round",
    "sandbox": "fresh /demo data; select weak prompts, start a three-item round, assert timer, reveal and rate by keyboard, then assert result"
  },
  {
    "id": "saved-plans",
    "claim": "The paid tool saves and reloads named round plans",
    "where": "paid tier, README",
    "test": "npm test -- --grep @claim:saved-plans",
    "sandbox": "fresh /demo data where paid plan behavior is enabled without checkout; save a named configuration, change it, reload it, and assert every setting"
  },
  {
    "id": "data-delete",
    "claim": "Deletes prompts, round notes, and saved plans from this device",
    "where": "workbench, privacy page",
    "test": "npm test -- --grep @claim:data-delete",
    "sandbox": "fresh /demo data; confirm deletion, assert the empty state, reload, and assert the data remains absent"
  },
  {
    "id": "free-core",
    "claim": "Import, tagging, practice, and CSV export need no license",
    "where": "landing facts, paid tier, README",
    "test": "npm test -- --grep @claim:free-core",
    "sandbox": "fresh browser with no license; import the shipped fixture and assert tag, practice, and export controls remain enabled"
  },
  {
    "id": "paid-price",
    "claim": "Named round plans cost $9 once through Sociobot checkout",
    "where": "landing facts, paid tier, terms, README",
    "test": "npm test -- --grep @claim:paid-price",
    "sandbox": "fresh landing page; assert the stated price, follow the displayed checkout endpoint without payment, and assert it begins a hosted checkout redirect"
  },
  {
    "id": "license-check",
    "claim": "A returned license is stored locally and checked at most once each day",
    "where": "privacy page, README",
    "test": "npm test -- --grep @claim:license-check",
    "sandbox": "fresh /demo URL with a fixture license response; assert token capture, URL cleanup, unlocked control, reload, and one verification request"
  },
  {
    "id": "license-data-minimization",
    "claim": "License checks send only the license token to Sociobot",
    "where": "privacy page, README",
    "test": "npm test -- --grep @claim:license-data-minimization",
    "sandbox": "fresh /demo data with a recorded verification response; seed a plan and license, reload, and assert exactly one GET to the documented verify URL with only the encoded license query and no body"
  }
]
